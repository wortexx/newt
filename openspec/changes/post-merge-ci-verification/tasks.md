# Tasks — post-merge-ci-verification

Ordering follows design.md's Migration Plan: inert workflow edits first, then the paid observation runs, then the backstop swap (only after the watchdog is observed), then docs. Tasks marked **[VM/EDA]** start the Azure VM and/or run multi-hour flow stages and are **user-triggered** (design D8); tasks marked **[Azure-destructive]** delete a live resource and are user-executed or explicitly user-approved. Everything unmarked is plain editing or read-only checks the agent does.

## 1. Workflow and doc edits (inert until dispatched — validated by the fast lane, no VM)

- [x] 1.1 Add `stop_after` and `resume_exclude` as optional `workflow_dispatch` inputs to `.github/workflows/pnr.yml` (design D2): `resume_exclude` feeds the existing `PNR_RESUME_EXCLUDE` workflow env (`${{ github.event.inputs.resume_exclude || '' }}`), `stop_after` is passed as `PNR_STOP_AFTER` on the "Place and route" step's `make` line; `PNR_GATE` is not exposed. Update the step's comment block and the env comments to describe the inputs and their precedence. Verify: `actionlint` passes; `git diff` shows no change to any job's `if:`/`needs:`/`runs-on:`; a `PNR_DRY_RUN=1` invocation of `run_pnr.sh` with `PNR_STOP_AFTER=grt_repair` against a scratch `save/` dir holding zips named for every stage through `grt` (minus `grt_repaired`) lists exactly `grt_repair` as the stage it would run.
  - Done 2026-09-13. `actionlint .github/workflows/pnr.yml` passed with no output. `git diff -- .github/workflows/pnr.yml | grep -E '^[+-].*(if:|needs:|runs-on:)'` matched nothing. Dry run against a scratch `save/` with zips for `power_grid pre_place gpl2 dpl cts grt` printed `Stage '<x>' already has checkpoint ... - resuming past it.` for floorplan/pre_place/gpl/dpl/cts/grt, then `[dry-run] would run stage 'grt_repair' ...` and `PNR_STOP_AFTER=grt_repair reached - would not run the remaining stages.`, exit 0 - exactly `grt_repair` listed as the stage it would run.
- [x] 1.2 Fix `pnr.yml`'s comment references to `openspec/changes/ci-pnr-lane/...` so they point at `openspec/changes/archive/2026-09-13-ci-pnr-lane/...` (design D9). Verify: `grep -n 'openspec/changes/ci-pnr-lane' .github/workflows/pnr.yml` returns nothing and every replaced path exists on disk.
  - Done 2026-09-13. `grep -n 'openspec/changes/ci-pnr-lane' .github/workflows/pnr.yml` returns nothing. All three replaced paths verified to exist: `openspec/changes/archive/2026-09-13-ci-pnr-lane/specs/pnr-flow/spec.md`, `.../design.md`, and the archive directory itself (for the runbook reference).
- [x] 1.3 Document the dispatch inputs in `docs/pnr-pipeline.md` (the `restore-checkpoints` row and wherever `PNR_STOP_AFTER`/`PNR_RESUME_EXCLUDE` are described) with the Run B invocation from design D1 as the worked example. Verify: the documented `gh workflow run` command's input names match `pnr.yml`'s `inputs:` keys exactly.
  - Done 2026-09-13. Updated the `restore-checkpoints` row and added a "Manual dispatch inputs" section with the Run B worked example. `pnr.yml`'s `inputs:` keys are `resume_from_run`, `resume_exclude`, `stop_after` — the documented `gh workflow run -f resume_from_run=<A> -f resume_exclude=grt_repaired -f stop_after=grt_repair` uses the same three names.
- [x] 1.4 Open the PR for 1.1–1.3 (title `ci(pnr): expose stop_after/resume_exclude dispatch inputs (infra-plan Phase 9)`) and merge it once `lint`/`sw` are green — the inputs must exist on `main` before Run B can use them. Verify: `gh api repos/wortexx/newt/actions/workflows/pnr.yml --jq .state` is `active` and `gh workflow view pnr.yml --yaml | grep -A3 'stop_after:'` shows the input on the default branch.
  - Done 2026-09-13. Opened [PR #21](https://github.com/wortexx/newt/pull/21) (branch `ci/pnr-dispatch-inputs`); `lint` and `sw` both passed, merged by the user at 2026-09-13T21:13:45Z (merge commit `cba119fe17081fb1f72b048962e6f561a86f0133`). `gh api repos/wortexx/newt/actions/workflows/pnr.yml --jq .state` → `active`. `gh workflow view pnr.yml --yaml | grep -A3 'stop_after:'` shows the input live on `main`.

## 2. Run A — fresh full run on the current netlist key (design D1)

- [x] 2.1 **[VM/EDA]** Obtain Run A: either let the Friday 2026-09-18 18:00 UTC cron fire (preferred, $0 extra, and it observes the post-merge schedule trigger for real) or, if the user wants Phase 9 closed sooner, `gh workflow run pnr.yml` with no inputs. Record the run ID, the trigger event, and start/stop wall-clock here. Verify: `gh run view <A> --json jobs` shows `start`, `restore-checkpoints`, `pnr`, `upload-checkpoints`, `stop` all `completed`; the `restore-checkpoints` log says "No resume requested"; the `pnr` log shows a synth cache **miss** (expected — the key moved with `include apm.mk`, design Context) followed by a "Cached … to /synth-cache/synth-…" save line; `pnr_status.log` in the `pnr-reports` artifact reads `grt ok`.
  - In progress: user dispatched `gh workflow run pnr.yml` (no inputs) 2026-09-13. Run A = **34783899813**, `event: workflow_dispatch`, started 2026-09-13T21:27:51Z. `start` and `restore-checkpoints` jobs completed `success` by 2026-09-13T21:29:48Z; `pnr` job went `in_progress` at that point (this is the window for task 2.2). Per-job log inspection (e.g. `restore-checkpoints`'s "No resume requested" line) is unavailable via `gh run view --log` until the whole run completes — confirmed this stays true even after an individual job (`pnr`) completes, as long as the overall run is not `completed`.
  - **2026-09-14T22:02:34Z: `pnr` job completed `success`.** `upload-checkpoints` went `queued` at that instant; `synth` (run 34820756433, the overlap-test run from 2.2/2.3) started 2s later at 22:02:36Z and got the runner first — this is design D3's **ordering (1)** (synth-before-upload), confirmed by `stop`'s guard log (`runner busy=0, synth.yml active=0, other pnr.yml active=0` → `Deallocate VM` ran directly). The guard's skip branch is not exercised by this run (task 3.3 still needed for that).
  - **Done 2026-09-15.** Run A `--json jobs` for the whole run: `start`/`restore-checkpoints`/`pnr`/`upload-checkpoints`/`stop` all `completed`/`success`, overall run `conclusion: success`. `restore-checkpoints` log: `"No resume requested - the pnr job will run the full flow from floorplan."` — exact match. `pnr` log: `"Cache miss for /synth-cache/synth-16d997ae59b007066bccc5be41cad4f8 - will synthesize."` then `"Cached 121M to /synth-cache/synth-16d997ae59b007066bccc5be41cad4f8"` — exact match. `pnr_status.log` (from the `pnr-reports` artifact): `floorplan ok`, `pre_place ok`, `gpl ok`, `dpl ok`, `cts ok`, **`grt ok`** (exact match), `grt_repair ok skipped`, `drt failed DRT-0206`, `drt failed exit=1 attempts=1`, `final skipped predecessor-failed` — matches design D1's forecast exactly ("ending in DRT-0206 as in bringup-10, no DEF"). No `basilisk-pnr-def` artifact was produced, consistent with that forecast. All of 2.1's stated verify criteria are satisfied; the checkpoint-loss finding below is a separate, additional discovery that doesn't undo this task's own literal verify text (which only requires job completion + these log/status lines, not that Blob checkpoints exist) but does block task 3.1 downstream.
  - **CRITICAL FINDING 2026-09-15 — Run A's checkpoints were destroyed by the overlap, not uploaded.** `upload-checkpoints`' log: `"No checkpoint .zip files found under .../target/ihp13/openroad/save - nothing to upload (pnr job may have failed before floorplan)."` — misleading message; `pnr` actually succeeded. Root cause: `pnr.yml` and `synth.yml` share the same self-hosted runner's workspace, and the overlap-test `synth` run's own `actions/checkout@v4` step ran at 22:02:40Z — 6s after `pnr` finished (22:02:34Z) — whose default `git clean -ffdx` wiped the untracked `target/ihp13/openroad/save/` (Run A's checkpoints) before `upload-checkpoints` got a chance to run (it didn't start until 00:31:40Z, 2.5h later, after `synth` itself finished). This is the same collision class `docs/pnr-pipeline.md`'s Notes section already documents for *manual* out-of-band work on the shared workspace — but here an *automated* sibling workflow's own job triggered it, which design D3 never considered (D3 only reasoned about job-scheduling order and VM-deallocation timing, not workspace destruction between the two lanes' jobs). **Consequence: Run A produced no Blob checkpoints, so task 3.1 (Run B resuming from Run A) is currently blocked.**
  - **User decision 2026-09-15: "Fix later, unblock now."** The shared-workspace collision itself is *not* fixed in this change (a real design decision — e.g. separate work directories per lane, or moving checkpoints outside the git workspace before any cross-job window — not a one-line patch, and out of this change's stated scope). To unblock task 3.1, a fresh checkpoint-source run is needed; to keep that fresh run from hitting the identical collision by accident, `synth.yml` was disabled again (`gh workflow enable` → `disabled_manually`, agent action, reversible per design D8) immediately after this finding, since its nightly `30 2 * * *` cron would otherwise very likely fire during the next ~23-26h full P&R run and reproduce the same wipe with no dispatch needed. Plan: (1) synth.yml stays disabled; (2) user dispatches a fresh no-input `pnr.yml` run (design D1's Run A, effectively "Run A2" since the original Run A already satisfies 2.1's own literal verify text) with nothing else running on the shared runner this time; (3) once it uploads real Blob checkpoints, dispatch Run B (3.1) against that run's ID; (4) re-enable `synth.yml` afterward. **Follow-up recorded for a future change** (not this one): fix the shared-workspace collision itself so P&R and synth lanes can safely overlap without an operator having to serialize them by hand — add to `docs/infra-plan.md`'s follow-ups list in task 6.4.
  - **Design gap found 2026-09-13**: attempting task 2.2's `gh workflow run synth.yml` failed with `HTTP 422: Cannot trigger a 'workflow_dispatch' on a disabled workflow` — `synth.yml` was still fully `disabled_manually` from bring-up (GitHub's enable/disable state is per-workflow, blocking all triggers including dispatch, not just the schedule), but design's Migration Plan step 2 has task 2.2 dispatching `synth.yml` *before* step 5's `gh workflow enable synth.yml` (task 5.3). Resolved by enabling `synth.yml` now, out of the plan's original order — design D8 assigns `gh workflow enable` to the agent ("reversible"), and the target end state (enabled) is the same either way, just reached earlier. `gh api .../actions/workflows/synth.yml --jq .state` confirmed `disabled_manually` → `active` at 2026-09-13. Task 5.3 will find the workflow already `active` when it comes up; its own verification still applies.
- [x] 2.2 **[VM/EDA]** While Run A's `pnr` job is `in_progress` on the runner: `gh workflow run synth.yml` (design D3). Verify immediately: `gh run list --workflow=synth.yml --limit 1 --json status` shows `queued` (not `in_progress` — the runner is held by P&R), and the next hourly `vm-watchdog.yml` run's log shows `VM is running and something is active or queued … leaving it up`. Record that watchdog run ID as the observed **busy-path** tick.
  - Done 2026-09-13. User dispatched `gh workflow run synth.yml` at 2026-09-13T21:37:08Z (after the design-gap fix above re-enabled the workflow). synth.yml run = **34784366126**, `event: workflow_dispatch`, confirmed `status: queued` — not `in_progress`, since Run A's `pnr` job held the runner. **Busy-path tick: vm-watchdog.yml run 34788375060**, completed 2026-09-13T22:59:49Z, log: `"VM is running and something is active or queued on the runner - leaving it up."` — exact match to the expected message. Task 2.2's own verification (queued state + busy-path tick) is satisfied by this run regardless of its eventual outcome — see 2.3's note for what happened to it afterward.
  - **Second-order consequence of the design-gap fix, found 2026-09-14**: enabling `synth.yml` (`gh workflow enable`) re-armed its `schedule` trigger (`cron: '30 2 * * *'`) along with `workflow_dispatch` — GitHub's enable/disable is per-workflow, not per-trigger, so there was no way to unblock the dispatch without also reviving the nightly cron ahead of task 5.3's planned re-enable. That cron fired (after GitHub's apparent ~5h scheduling lag observed on this repo historically) at 2026-09-14T08:03:21Z as run **34820756433** (`event: schedule`), and `synth.yml`'s own `concurrency: group: synth-${{ github.ref }}, cancel-in-progress: true` cancelled the still-queued dispatched run **34784366126** on collision — it never got the runner, so it never actually exercised the coexistence guard. The new scheduled run 34820756433 is itself now queued behind Run A's `pnr` job, i.e. sitting in the same overlap condition.
  - **Resolution (user decision 2026-09-14)**: adopt run **34820756433** (`event: schedule`) as the operative overlap-test synth run for task 2.3, rather than re-dispatching (which would just cancel it again via the same concurrency group for no benefit). It costs nothing extra (VM already up for Run A) and exercises the identical coexistence-guard mechanism; it also incidentally gives real early data toward task 5.4's question. Tracking it to completion.
- [x] 2.3 After Run A and the synth run both complete: read `stop`'s guard output (`gh run view <A> --log --job <stop-job-id>`) and record which D3 ordering occurred — (1) synth ran before `upload-checkpoints` and `stop` deallocated directly (`runner busy=0, synth.yml active=0 …` then `Deallocate VM` ran), or (2) `stop` logged `Another job is running or queued on the shared runner - leaving the VM up` and skipped. Verify: the synth run concluded `success` and its `basilisk-netlist`/`synth-reports` artifacts exist (`gh run view <synth> --json artifacts`); the P&R run's outcome is unaffected by the overlap (still `success`). This closes the coexistence-guard item **regardless** of which ordering occurred; note in this task whether the skip branch itself was seen (if not, 3.3 covers it).
  - **Done 2026-09-15. Ordering (1) — synth-before-upload.** `stop` job log (job 104206164175): `"runner busy=0, synth.yml active=0, other pnr.yml active=0"` then the `Deallocate VM` step ran (`az vm deallocate ...`) — the direct-deallocate path, confirming ordering (1) as already noted under 2.1/2.2. Skip branch **not** seen here → task 3.3 still needed. Synth run (34820756433) concluded `success` with both `basilisk-netlist` (12,769,703 bytes) and `synth-reports` (5,440,480 bytes) artifacts present (`gh api .../artifacts`). Run A's own outcome was unaffected by the overlap — `pnr` job itself still `success` (the overlap only cost Run A its *checkpoints*, not its job status — see the critical finding under 2.1, which is this task's one caveat: "unaffected" holds for the GitHub-reported verdict, not for the Blob artifacts a later resume needs).

## 3. Run B — dispatch that resumes Run A and stops after one stage (design D1/D2)

- [x] 3.1 **[VM/EDA]** Dispatch: `gh workflow run pnr.yml -f resume_from_run=<A> -f resume_exclude=grt_repaired -f stop_after=grt_repair`. Record the run ID and wall-clock (expected ≈ 15 min, ≈ $0.35). Verify in the logs: `restore-checkpoints` prints `RESUME MODE: restoring checkpoints from run <A>` and stages N zips; the `pnr` job's restore step prints `Netlist identity matches (synth-…) - safe to resume` (the guard's **matching** path, on a run whose marker came from a real full run) and `excluded: basilisk.grt_repaired.zip`; the synth cache **hits** (`Cache hit: restored …`, `make synth-all` skipped); `pnr_status.log` shows `skipped resume` for `floorplan`…`grt`, then `grt_repair ok` and `grt_repair stop-after`; the run concludes `success`; `upload-checkpoints` uploaded a fresh `synth-key.txt`. This closes the "real `workflow_dispatch` run" and "`resume_from_run` input path" items together.
  - **In progress 2026-09-16T13:13:09Z.** Dispatched at the user's explicit request (agent-executed, per their choice): `gh workflow run pnr.yml -f resume_from_run=34938462965 -f resume_exclude=grt_repaired -f stop_after=grt_repair`. Run B = **35100509927**, `event: workflow_dispatch`. `<A>` here is checkpoint-source run **34938462965** (the original Run A 34783899813's checkpoints were destroyed — see 2.1's critical finding — and this dispatch also validates `pnr-checkpoints-outside-workspace`'s checkpoint-export fix (its task 4.1) under a real run: PR #25 merged the fix to `main` before this dispatch, confirmed live via `gh workflow view pnr.yml --yaml | grep -c pnr-export` = 4. Pre-dispatch checks: `synth.yml` still `disabled_manually`, nothing else queued/in_progress on `pnr.yml`/`synth.yml`. Tracking to completion.
  - **Done 2026-09-16T13:25:01Z.** Wall-clock ≈11m52s (13:13:09Z → 13:25:01Z), well under the ≈15 min estimate. `restore-checkpoints` log: `##[warning]RESUME MODE: restoring checkpoints from run 34938462965.` then `Staged 11 checkpoint(s) into /home/newt/pnr-restore/34938462965` — exact match. `pnr` job's restore step: `Netlist identity matches (synth-16d997ae59b007066bccc5be41cad4f8) - safe to resume.` (the matching path) and `excluded: basilisk.grt_repaired.zip (its stage will re-run)` — exact match. Synth cache: `Cache hit: restored 121M from /synth-cache/synth-16d997ae59b007066bccc5be41cad4f8` — `make synth-all` did not run (step absent from the job's step list for this run). `pnr_status.log`: `floorplan skipped resume`, `pre_place skipped resume`, `gpl skipped resume`, `dpl skipped resume`, `cts skipped resume`, `grt skipped resume`, `grt_repair ok skipped`, `grt_repair stop-after` — exact match (`ok skipped` because `PNR_SKIP_GRT_REPAIR=1` is still hardcoded in the workflow's Place-and-route step, unaffected by the new dispatch inputs, exactly as design D2 intended). Run concluded `success` (`start`/`restore-checkpoints`/`pnr`/`upload-checkpoints`/`stop` all `success`). `upload-checkpoints` uploaded a fresh `synth-key.txt` alongside all 11 zips to `pnr-checkpoints/35100509927/`. **This closes both "real `workflow_dispatch` run" and "`resume_from_run` input path" together, and simultaneously serves as `pnr-checkpoints-outside-workspace` task 4.1's real-run verification**: the `pnr` job's "Export checkpoints out of the shared workspace" step logged `Exporting checkpoints from target/ihp13/openroad/save (3.3G) to /pnr-export/35100509927` then `Exported 11 checkpoint(s) in 0s, 1.5G total:` listing `basilisk.grt_repaired.zip (234810188 bytes)` among the full set with sizes; `upload-checkpoints` logged `Uploading 11 checkpoint(s), 1.5G total, from /home/newt/pnr-export/35100509927` and all 11 blobs plus `synth-key.txt` landed at `https://newtpnrcheckpoints.blob.core.windows.net/pnr-checkpoints/35100509927/...` with no removal-failure warning (silent success). Cross-reference: this evidence should also be copied into `pnr-checkpoints-outside-workspace`'s own task 4.1 note.
- [x] 3.2 Confirm the inputs are inert when empty: Run A's `pnr` log (2.1) shows `PNR_STOP_AFTER` unset/empty and the restore step did not run, i.e. a no-input run is byte-for-byte the pre-change behaviour. Verify: quote the relevant log lines here.
  - Done 2026-09-16. Run A (34783899813) `pnr` job's "Place and route" step invocation line: `PNR_TIMEOUT_GRT=57600 PNR_TIMEOUT_GRT_REPAIR=57600 PNR_SKIP_GRT_REPAIR=1 PNR_SKIP_ANTENNA_REPAIR=1 PNR_DRT_END_ITER=8 PNR_STOP_AFTER= make -C target/ihp13/openroad -f openroad.mk run-pnr PROJ_NAME=basilisk` — `PNR_STOP_AFTER=` is empty. Step 13, "Restore checkpoints to resume (only when a resume run is named)": `"conclusion":"skipped"`. Both confirm a no-input dispatch behaves exactly as it did before this change's inputs existed.
- [x] 3.3 **[VM/EDA]** Only if 2.3 recorded ordering (1): observe the `stop` guard's skip branch deterministically by dispatching `synth.yml` the moment a slice run's `upload-checkpoints` job starts (`gh run watch <B'>` on a second Run-B-shaped dispatch, or reuse 3.1 if timing allows). Verify: that run's `stop` log contains `leaving the VM up` and the `Deallocate VM` step is `skipped`; the queued synth run then executes to `success`. Skip this task with a note if 2.3 already saw the skip branch.
  - **Done 2026-09-16.** `synth.yml` re-enabled (agent action, reversible per D8 — was disabled since the 2.1 finding); dispatched a second slice run B' = **35105723732** (same shape as Run B, `-f resume_from_run=34938462965 -f resume_exclude=grt_repaired -f stop_after=grt_repair`) at 14:01:30Z. A monitor script polled B's job list every 5s and, the instant `upload-checkpoints` reported `in_progress` (14:09:55Z), dispatched `synth.yml` itself — run **35106656087**, queued 14:09:57Z, 2s later. B''s `stop` guard log: `"runner busy=0, synth.yml active=1, other pnr.yml active=0"` → step `Leave VM up for the other job` ran (`conclusion: success`) logging `"Another job is running or queued on the shared runner - leaving the VM up. The hourly vm-watchdog.yml will deallocate it once everything is idle."`; step `Deallocate VM` `conclusion: skipped`. Both exact matches to this task's verify text. The queued synth run picked up the runner immediately (`in_progress` right after B''s `stop` finished) — tracking it to `success`. Bonus: since the VM is now left up, the next `vm-watchdog.yml` tick (once synth finishes and nothing else queues) is a live shot at task 4.1's idle-deallocate observation.

## 4. Watchdog idle-deallocate (design D4) — must be observed before anything in §5

- [ ] 4.1 **[VM/EDA]** Observe one scheduled `vm-watchdog.yml` run that actually executes `Deallocate idle VM`. Natural source: the first tick after a run that left the VM up (ordering (2) in 2.3, or 3.3). Fallback if every run so far deallocated via `stop`: user runs `az vm start -g newt-synth-lane-rg -n newt-synth-runner` with nothing queued (`gh run list --status queued` empty) and waits for the next top-of-hour tick. Verify: `gh run view <tick> --log` shows `VM is running and idle - deallocating.` with the `Deallocate idle VM` step `success`, and `az vm get-instance-view … --query "instanceView.statuses[?starts_with(code,'PowerState/')].code" -o tsv` returns `PowerState/deallocated` afterwards. Record the tick's run ID and timestamp here — `vm-watchdog.yml`'s header and `docs/infra-plan.md` Phase 9 cite it.

## 5. Backstop swap and schedules (design D5/D6/D7) — ordered, after 4.1

- [ ] 5.1 **[Azure-destructive]** Delete the auto-shutdown schedule: `az resource delete -g newt-synth-lane-rg --resource-type Microsoft.DevTestLab/schedules -n shutdown-computevm-newt-synth-runner` (confirm `status: Disabled` with `az resource show` immediately before, as a last sanity check that nothing re-enabled it). Verify: `az resource list -g newt-synth-lane-rg --query "sort_by([].{name:name,type:type},&name)" -o table` lists **nine** resources, each mapping to a `main.bicep` declaration or the OS-disk row in `infra/azure/README.md`'s table, and no `Microsoft.DevTestLab/schedules` entry; `az vm get-instance-view` shows no auto-shutdown status. Record the deletion timestamp.
- [ ] 5.2 Confirm the weekly P&R cron is live and record the first scheduled run (design D7): `gh api repos/wortexx/newt/actions/workflows/pnr.yml --jq .state` is `active`; if 2.1 used the Friday cron, cite that run's `event: schedule` here — otherwise cite the first `schedule`-event run that appears after 2026-09-18 18:00 UTC. Verify: `gh run list --workflow=pnr.yml --json event,createdAt` contains a `schedule` entry. Put the open cost question (design Open Questions, first bullet) to the user explicitly in this task's notes with the measured cost of Run A.
- [ ] 5.3 Re-enable the synth lane's schedule: `gh workflow enable synth.yml`. Verify: `gh api repos/wortexx/newt/actions/workflows/synth.yml --jq .state` is `active`.
- [ ] 5.4 Observe what the nightly synth schedule does against a deallocated VM over the next 2–3 nights (design D6) and record it here: for each 02:30 UTC run, its final status (`queued` → `cancelled` by the next night's run, `failure` from GitHub's queue limit, or `success` because the VM happened to be up), whether it delayed or ran alongside any P&R run, and whether any `vm-watchdog.yml` tick or `pnr.yml` `start` job misbehaved because of the queued run. Verify: `gh run list --workflow=synth.yml --limit 5 --json event,status,conclusion,createdAt` quoted here with a one-line reading. If the behaviour is worse than D6's expectation, `gh workflow disable synth.yml` again and raise D6's open question with the user rather than patching `synth.yml` here.

## 6. Documentation truth-maintenance (design D9) — after §5 so every statement is true when written

- [ ] 6.1 Rewrite `.github/workflows/synth.yml`'s header note (1): the VM is deallocated by default, started by `pnr.yml`'s `start` job or by hand, deallocated by `pnr.yml`'s guarded `stop` and the hourly `vm-watchdog.yml`; the fixed auto-shutdown is gone (cite 5.1's date); state the nightly-schedule behaviour recorded in 5.4. Verify: `grep -n -i 'auto-shutdown\|10:00\|MANUAL\|Phase 6 automates' .github/workflows/synth.yml` returns nothing stale; `actionlint` passes (comment-only change, but keep the gate).
- [ ] 6.2 Update `.github/workflows/vm-watchdog.yml`'s header to past tense: the auto-shutdown was removed on 5.1's date after run 4.1 observed the idle-deallocate. Verify: `grep -n 'is removed once' .github/workflows/vm-watchdog.yml` returns nothing; `actionlint` passes.
- [ ] 6.3 Update `infra/azure/README.md`: drift table drops the schedule row (nine resources), the "deliberately undeclared … Phase 9 deletes it" prose becomes a one-line note that the schedule was deleted on 5.1's date, and the cost-guardrails paragraph names `vm-watchdog.yml` and `pnr.yml`'s `stop` as the only deallocation mechanisms. Verify: `grep -n -i 'exception\|DevTestLab\|shutdown' infra/azure/README.md` shows only the historical note.
- [ ] 6.4 Update `docs/infra-plan.md`: Phase 5's History paragraph and Phase 6's "one undeclared resource" paragraph to the post-deletion reality; every Phase 9 checkbox ticked with the observing run ID/date from §2–§5; the "Azure cost creep" Risks row to mention that the fixed auto-shutdown no longer exists; add the synth-cache-key over-inclusion finding (design Risks) to the Risks table; mark Phase 9 ✅ in its heading and in the phase-ordering diagram. Verify: `grep -n -i 'auto-shutdown\|disabled by hand\|Still open' docs/infra-plan.md` shows only historical or Phase-11 references, and no Phase 9 checkbox remains `[ ]`.
- [ ] 6.5 Open and merge the docs PR for 6.1–6.4 (title `docs(ci): Phase 9 closed — watchdog observed, auto-shutdown deleted`). Verify: `lint`/`sw` green; `infra.yml` did not run (no `infra/**` template touched — only its README) or ran green if it did.

## 7. Wrap-up

- [ ] 7.1 `openspec validate post-merge-ci-verification --strict` passes and every task above carries its run IDs, dates, and the ordering/observations it asked for; then hand off to `/opsx:archive` (which syncs the two delta specs into `openspec/specs/`). Verify: validate exits 0; `git status` clean after the archive commit.
