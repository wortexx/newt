# Upgrade yosys to upstream v0.69 — tasks

Tags: **[edit]** = plain file editing, **[docker]** = needs a Docker image build (CI PR run or local, minutes to ~30 min for the yosys stage), **[eda]** = short yosys run inside the image (minutes, small module), **[long-run]** = full `synth-all` on the self-hosted runner (~2.5 h, >30 GB RAM). Cheap gates (build success, smoke-test assertions, parser unit run on a saved report) are the completion criteria everywhere except the adoption gate in section 5.

## 1. Image: upstream yosys v0.69

- [x] 1.1 **[edit]** Rewrite `docker/yosys/Dockerfile` per design D1: `ARG YOSYS_VERSION=v0.69`, fetch the release `yosys.tar.gz` from GitHub releases, configure with `cmake -B build` (clang, `CMAKE_INSTALL_PREFIX=/build`), build with `-j$(nproc)`, `cmake --install build`; drop `YOSYS_REPO`/`YOSYS_COMMIT`. Add `cmake`, `lld`, `libfl-dev`, `gawk` to `docker/yosys/packages.txt`. Verify by review that no reference to `phsauter/yosys` or `3ce5059` remains under `docker/` (`grep -rn "phsauter/yosys\|3ce5059" docker/` is empty). — done. Two corrections to D1 found by inspecting the real v0.69 asset: (a) the release `yosys.tar.gz` has **no top-level directory** (members start at `CMakeLists.txt`, `abc/`, `libs/`), so the old `--strip-components=1` had to be dropped — keeping it would have silently unpacked nothing; (b) upstream's own reference build passes `-DYOSYS_USE_BUNDLED_LIBS=ON`, adopted here because it makes the offline build deterministic instead of relying on `find_package` failing quietly. Also set `-DBUILD_SHARED_LIBS=OFF` (libyosys would otherwise be a shared library that `YOSYS_INSTALL_LIBRARY=OFF` never installs; components are OBJECT libraries so the driver stays self-contained for the copy-only runner stage) and `-DYOSYS_ENABLE_UNIT_TESTS=OFF`. Confirmed the asset bundles every submodule (abc 2448 entries, libs/slang 750, frontends/slang 195) so configure needs no network, and that ubuntu:24.04's cmake 3.28.3 meets upstream's `cmake_minimum_required(3.28)`. Verified: the grep is empty.
- [ ] 1.2 **[docker]** Open the PR so `docker-image.yml` builds the yosys stage and the composite. Verify the yosys stage builds and the existing smoke test passes (`yosys --version` prints `0.69`). If the clang build fails, switch the stage to gcc-13 per design risk note before trying anything else. — **first attempt (run 35192327059) built everything but failed the smoke test; fixed, rerunning.** The yosys stage itself built clean in 27m35s under clang, so the gcc-13 fallback was not needed and the Dockerfile rewrite (tarball without `--strip-components`, CMake, bundled submodules) is sound. Two defects surfaced, both fixed in the same push:
  1. **Pre-existing latent bug in `docker-image.yml`, first exposed by this change.** The composite consumed stage images by the mutable `:ci` tag, and buildx does not re-resolve a tag whose resolution it has cached. Log evidence: the yosys stage pushed `sha256:688531e8` while the composite resolved `sha256:5f4a7521` and copied the OLD fork's `/build` into the image the smoke test then ran. Harmless for as long as no stage ever changed, which is why it went unnoticed. Fixed by giving each stage step an `id` and consuming it by `@${{ steps.<id>.outputs.digest }}`, which also stops two concurrent PR runs clobbering each other through the shared `:ci` tag. This is scope beyond the task as written, but task 1.2 cannot pass without it.
  2. **A bug in my own task 1.3 assertion.** `yosys -p 'help <unknown>'` exits 0 (`kernel/register.cc` logs "No such command or cell type" via `log()`, not `log_error()`), so the `read_slang` check passed against *any* yosys — it reported OK against the stale 0.40 image in the very run where the other two assertions correctly failed. Now asserts on the absence of that message. The smoke test also prints observed values on failure; the original bare `FAIL` lines cost a full CI round trip to diagnose.
- [x] 1.3 **[edit]** Extend `docker/smoke-test.sh` with the two feature assertions from the spec: `yosys -p 'help abc'` output contains `-liberty_args`, and `yosys -p 'help read_slang'` exits 0 without loading a plugin. Verify the PR smoke test passes with both checks and that each check fails loudly when its grep target is altered locally (one-off negative test, not committed). — done, with a third assertion added: the pinned **version** itself. Justification: the pre-existing bare `yosys --version` check passes identically against the old fork and v0.69, so without it a stale or wrong image layer would pass the smoke test silently — demonstrated below. Verified locally by running the real `docker/smoke-test.sh` with stub tools on PATH: against a v0.69-shaped yosys stub all three new checks pass and the script exits 0; against an old-fork-shaped stub (0.40, `help abc` without `-liberty_args`, `help read_slang` exiting 1) all three fail loudly and the script exits 1, while the old bare check still reported OK. In-image confirmation rides with task 1.2.
- [x] 1.4 **[edit]** Add the `pr-<number>` composite push to `docker-image.yml` for pull-request runs (design D5) — same image the smoke test ran against, tagged `ghcr.io/wortexx/newt-eda:pr-<n>`. Verify the PR run pushes it and `docker pull ghcr.io/wortexx/newt-eda:pr-<n>` succeeds anonymously. — done. Added a `Publish PR image for gate runs` step gated on `github.event_name == 'pull_request'`, placed after the smoke test so an image that fails it is never published, and mutually exclusive with the existing `Tag and publish` (`!= 'pull_request'`) so a PR can never move `:dev`. `actionlint` clean. The push/pull confirmation needs a real PR run (blocked with 1.2).

## 2. Flow: remove the last fork dependency

- [ ] 2.1 **[eda]** Spike per design D3 step 1 inside the `pr-<n>` image: synthesize one small kept-hierarchy module (e.g. the UART or GPIO instance's module, extracted via `hierarchy -top` on the pickled `basilisk.sv2v.v`) through `yosys_synthesis.tcl`'s combinational path twice — once with the `write_blif`/`read_lib`/`read_blif` round-trip removed from `abc-speed-opt-new.script`, once with it substituted to a fixed `$work_dir` path — and diff ABC's `stime`/`print_stats` output and the resulting cell counts. Record the numbers in this task. Verify: a written conclusion of "round-trip has no effect, delete" or "round-trip still matters, patch".
- [ ] 2.2 **[edit]** Apply the spike outcome: either delete the three round-trip lines (and the "bug in buffer" comment) from `target/ihp13/yosys/scripts/abc-speed-opt-new.script`, or take design D3's option 2 (patch) or option 3 (fixed scratch path plus `YOSYS_MAX_THREADS=1`). **Note the revised D3 cost estimates recorded during task 1.2**: the patch is not the ~10-line rebase originally assumed, because upstream never reads a file-based `-script` at all (it sources the user's own path), so the fork's whole read-substitute-rewrite shape has to come back inside v0.69's threaded tempdir split; and option 3's thread cap is cheaper than it first looked, since ABC here was already effectively single-threaded. Prefer option 1, then 3, then 2. Verify `grep -rn "{tmpdir}" target/ihp13/yosys/scripts/` is empty in the delete case, or the PR image build applies the patch cleanly in the patch case.
- [x] 2.3 **[edit]** Confirm by review against upstream v0.69 `help` output that every yosys command and option in `yosys_synthesis.tcl`, `yosys_common.tcl`, `yosys_hier_top_synth.tcl`, `yosys_hier_part_synth.tcl` and `yosys_gen_hier_list.tcl` still exists (`abc -constr/-dff/-showtmp/-liberty_args`, `dfflibmap -prepare`, `hilomap -singleton`, `stat -tech cmos/-json`, `write_verilog -attr2comment/-noexpr/-nohex/-nodec`, `splitnets -format`, `autoname`, `attrmvcp -copy`). Verify: a checklist in this task with each command marked present, run as `yosys -p 'help <cmd>'` inside the `pr-<n>` image. — done, **by mechanical check against the pinned v0.69 source tree** rather than `help` output in the image (which is unavailable until 1.2; the help text is generated from exactly this source, and checking the source covers every command at once instead of by hand). All **36 commands** the five scripts invoke are present: abc, attrmap, attrmvcp, autoname, blackbox, booth, check, clean, dfflibmap, flatten, fsm, hierarchy, hilomap, memory, memory_collect, memory_map, opt, opt_clean, opt_dff, opt_expr, peepopt, pmux2shiftx, proc, read_liberty, read_verilog, rename, select, setattr, setundef, share, splitnets, synth, techmap, tee, wreduce, write_verilog (the last registered as `Backend("verilog")`, the `write_` prefix being added by the base class). All options used are present too: `abc -liberty/-liberty_args/-constr/-dff/-showtmp/-D/-script`, `dfflibmap -liberty/-prepare`, `stat -top/-liberty/-tech/-json/-width`, `write_verilog -norename/-noattr/-noexpr/-nohex/-nodec/-attr2comment`, `splitnets -ports/-format/-driver`, `hilomap -hicell/-locell/-singleton`, `attrmvcp -copy/-attr`, `attrmap -rename`, `rename -wire/-suffix/-src`, `select -list/-write/-module`, `setattr -mod/-set`, `opt_dff -sat/-nodffe/-nosdff`, `opt -fast/-full/-nodffe/-nosdff/-purge`, `opt_clean -purge`, `clean -purge`, `fsm -fm_set_fsm_file`, `hierarchy -check/-top`, `read_verilog -sv/-overwrite`, `read_liberty -lib`, `tee -q/-o/-a`. Nothing the flow uses was removed in the 0.40→0.69 span. The one absent name is `lsoracle`, a third-party plugin the script already hard-disables (`&& 0`) — not a regression.

## 3. Metrics: parse `stat -json`

- [x] 3.1 **[edit]** Add the JSON twin of the area report to `target/ihp13/yosys/scripts/yosys_synthesis.tcl`: `tee -q -o "${report_dir}/${proj_name}_area.json" stat -json -top $top_design {*}$liberty_args`, next to the existing `.rpt` line. Verify by running the combinational script on the task-2.1 module and confirming `<module>_area.json` parses as JSON with `num_cells`, `area` and `num_cells_by_type` for the top module. — done. Added next to the existing `.rpt` line. Established from v0.69's `passes/cmds/stat.cc` exactly what this emits: with `-top` and **without** `-hierarchy`, the whole-hierarchy rollup lands under a top-level `"design"` key in flat form (`num_cells` a plain integer, `area` a separate float, `num_cells_by_type` integer counts). With `-hierarchy` those become objects of *stringified* counts instead — a shape the parser now rejects loudly rather than misreading (see 3.2). Real-output confirmation needs a yosys run (blocked with 2.1).
- [x] 3.2 **[edit]** Port `target/ihp13/yosys/scripts/synth_metrics.py` to read the JSON report (design D4): cells = top module's including-submodules `num_cells`, chip area = top `area`, DFFs = sum of `num_cells_by_type` entries with the `sg13g2_df` prefix (still excluding `sg13g2_sdf*`); keep the `check` and STA parsers unchanged; `--area` now expects the `.json`. Verify with a unit run against the task-3.1 JSON and against a hand-written two-module JSON fixture confirming no double counting; verify the old `.rpt` input is rejected with a clear error. — done. Parser now slices the first `{` to the last `}` (so stray yosys log lines captured by `tee` do not break it) and reads the `"design"` rollup. Verified locally with fixtures built to match v0.69's emitter: a two-module fixture returns the design rollup (1550 cells) rather than anything derived from the per-module entries, counts DFFs as 610 while correctly excluding both the scan-DFF variant `sg13g2_sdfrbp_1` and the submodule-type entry that `stat` merges into `num_cells_by_type`. Four rejection paths each give a named, actionable error: the old text `.rpt` (names the `.json` to pass instead), the `-hierarchy` shape, a report generated without `-top`, and one generated without `-liberty`. Real-run JSON still to come with 5.1.
- [x] 3.3 **[edit]** Update `.github/workflows/synth.yml`: pass `reports/basilisk_area.json` to the metrics step, and add the `workflow_dispatch` input `image_tag` (default `dev`) feeding the job's `container:` image (design D5). Verify by review and by the dispatch in task 5.1 actually running on `pr-<n>`. — done. `--area` now points at `basilisk_area.json`; added the `image_tag` dispatch input (default `dev`) feeding `container:` via `${{ inputs.image_tag || 'dev' }}`, which falls through to `dev` for schedule and pull_request events. `actionlint` clean. Live dispatch proof comes with 5.1.

## 4. Naming check

- [x] 4.1 **[edit]** Add `target/ihp13/yosys/scripts/check_netlist_names.py` (design D6): given the netlist path, grep for every literal instance/net pattern used by `target/ihp13/openroad/src/basilisk_instances.sdc`, `target/ihp13/openroad/scripts/macros*.tcl` and the `*_reg` clock-pin lookup; exit non-zero naming any pattern with zero matches, except patterns listed as known-broken (`*ddr_rcv_clk_o*`) which are reported as warnings. Verify with a unit run against a fixture netlist containing all patterns (passes) and one missing pattern (fails naming it). — done, built to *parse* the patterns out of the backend scripts rather than hard-code them, so the check cannot drift from what the backend actually matches: it resolves `set VAR` definitions and `$VAR` references transitively, then collects `get_cells`/`get_nets` globs, `get_fanin -to`/`get_fanout -from` anchors, and any `set` value that is itself an object path. That yields **122 unique patterns** from the four Tcl sources, covering every SRAM instance path in `macros*.tcl` individually. Separators are matched loosely (`.` or `/` interchangeable) because yosys emits `.` for flattened hierarchy while `/` survives preserved module instances, sometimes within one path — strictness here would produce false failures. Verified: a generated fixture containing all patterns passes (exit 0, one known-unmatched warning for `*ddr_rcv_clk_o*`); a fixture with delay-line instances removed exits 1 and names all five affected patterns with their source file and Tcl variable; a separator-flexibility check matches `.` and `/` forms with a working negative control; a missing netlist errors clearly.
- [x] 4.2 **[edit]** Wire the check into `synth.yml` after `synth-all`, non-gating for the lane's normal runs (`continue-on-error`) but surfaced in the summary, and gating when dispatched for an adoption gate (`image_tag != dev`). Verify by review; real proof is task 5.1. — done. Runs right after `synth-all`, tees into the step summary, and is `continue-on-error: ${{ (inputs.image_tag || 'dev') == 'dev' }}` — advisory on routine lane runs, gating on an adoption-gate dispatch. `actionlint` clean; gating logic verified by evaluating the expression for `dev`, `pr-42` and unset.

## 5. Adoption gate and baseline reseed

- [ ] 5.1 **[long-run]** Dispatch `synth.yml` with `image_tag=pr-<n>` on the self-hosted runner. Verify: `synth-all` exits 0, yosys `CHECK` reports 0 problems, the naming check passes (with the pre-existing `*ddr_rcv_clk_o*` warning noted), and the metrics summary reports cells / area / DFFs against the fork-era baseline (714,166 / 17,156,844.69 / 89,258). Record the numbers, the wall time versus ~2h28m, and the delta explanation in this task. If the delta is unexplained and worse, stop here and decide with the previous image still as `:dev`.
- [ ] 5.2 **[edit]** Reseed `target/ihp13/yosys/synth-baseline.json` from the 5.1 run (design D7): new cells / area / DFFs, `wns_ps` as measured or unavailable, `source` = this change, `date`, `image` = the `pr-<n>` tag, new `yosys` field `v0.69`. Verify `synth_metrics.py` run against the 5.1 JSON with the new baseline reports zero drift on all three metrics.

## 6. Docs and rule retirement

- [x] 6.1 **[edit]** Update `AGENTS.md` (tool list line: upstream yosys v0.69 pinned in `docker/yosys/Dockerfile`, no fork, no PR-4343 note; constraints bullet: "bump via a change with the adoption gate"), `README.md` (same; drop the "must include PR-4343 / no release has it" sentences), and the `openspec/config.yaml` context line for the flow. Verify `grep -rn "phsauter/yosys\|PR-4343\|do not rebase" AGENTS.md README.md openspec/config.yaml` is empty. — done. All three updated; the constraints bullet in `AGENTS.md` now states the bump rule (re-run the adoption gate, reseed the baseline) rather than the retired do-not-rebase rule. Verified: the grep is empty. Diffs are minimal (README: 2 lines → 1); the markdown-lint warnings the editor shows on `README.md` are pre-existing (hard tabs, consecutive blank lines) and untouched.
- [ ] 6.2 **[edit]** Update `docs/infra-plan.md`: Phase 1 assumptions row for yosys (fork retired, v0.69, date), Phase 1 "Kept pinned" bullet, Phase 4 note that metrics parse the JSON report, Phase 8 preamble stating the ≥ 0.67 requirement is met by this change, and a Risks row for the QoR delta recorded in 5.1 with the previous baseline block kept for the record. Verify the Phase 8 checklist is unchanged (its prototype task belongs to the next change).
- [ ] 6.3 **[edit]** After merge, once `docker-image.yml` publishes from `main`: update `synth-baseline.json`'s `image` field to the published `<date>-<sha>` tag and confirm the first `main` synth-lane run reports ~zero drift. Verify by the run summary; if drift is non-zero, investigate the moving-tag switch first (design D7).
