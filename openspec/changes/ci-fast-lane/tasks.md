# Tasks — CI Fast Lane

Tool legend: **[edit]** = plain file editing, verifiable without EDA tools; **[eda]** = needs the `newt-eda` container (Verilator/bender/riscv toolchain), can be validated locally with `docker run` before pushing; **[gh]** = needs GitHub repo-admin access (branch protection). No synth/P&R runs anywhere in this change.

## 1. Workflow scaffold

- [x] 1.1 **[edit]** Create `.github/workflows/ci.yml`: `on: [push, pull_request]`, `container: ghcr.io/wortexx/newt-eda:dev`, `runs-on: ubuntu-latest`, five jobs (`lint`, `sw`, `sim-unit`, `sim-soc`, `synth-coproc`) each with a checkout step and a placeholder `echo "TODO"` body. Verify: workflow YAML is valid (`gh workflow view` or a YAML linter) and a pushed commit shows all five job names appear as pending/running checks on the commit/PR.
      Went straight to real/stub bodies rather than a separate placeholder pass (tasks 2-3 below), then verified via two real pushes to `feat/ci-fast-lane`. First push (commit `676bef3`, run `33556375472`): all five job names appeared immediately as checks; `sim-unit`/`synth-coproc`/`sim-soc` succeeded, `sw` succeeded, `lint` failed - a real, useful finding (see 2.1). Second push (commit `20e0b81`, run `33557013253`) after the fix: all five green. YAML parsed clean with `python3 -c "import yaml; yaml.safe_load(...)"` both times.

## 2. Real jobs

- [x] 2.1 **[eda]** Implement `lint`: `bender sources` (the real dependency-graph-consistency check — `bender check`, named in this task's original draft and in `docs/infra-plan.md`'s Phase 3 table, does not exist in any bender version; found via a real CI failure, see completion note); for changed `.sv`/`.svh` files (`git diff --name-only` against the PR base or push before-SHA, per design.md D2), run `verible-verilog-lint` and a per-file `verilator --lint-only`. Non-zero exit on any error-level finding from any of the three. Verify locally first: `docker run --rm -v $PWD:/work -w /work ghcr.io/wortexx/newt-eda:dev bash -c '<lint commands>'` against a clean checkout (exits 0) and against a deliberately introduced lint error (exits non-zero).
      **Found a real bug via the first real CI run, not local testing**: `bender check` doesn't exist in *any* bender version (confirmed against both the image's pinned 0.27.4 and a newer 0.32.0) — it was a stale, never-actually-run claim already sitting in `docs/infra-plan.md`'s Phase 3 table before this change, which the planning artifacts inherited without independently verifying it. My local pre-push testing of this exact step used the host's own (newer) bender rather than the pinned container one, so it didn't catch this; the real GitHub Actions run did (`error: Unknown command 'check'.`, run `33556375472`). Fixed by switching to `bender sources` (same intent - fully resolves `Bender.yml`/`Bender.lock`, errors on genuine inconsistency) everywhere the wrong claim had propagated: `ci.yml`, `proposal.md`, `design.md`, `specs/ci-pipeline/spec.md`, this task, and the original `docs/infra-plan.md` table row. Verified for real: `verible-verilog-lint` found an actual finding on `hw/iguana_soc.sv` (trailing whitespace) in local pre-push testing against `ghcr.io/wortexx/newt-eda:dev`; `verilator --lint-only` resolves cheshire/axi typedefs correctly once fed `bender script verilator`'s own `+incdir+`/`+define+` output (confirmed both locally, once run and consumed in the *same* container - mixing a host-generated flag file with a container run breaks path resolution, a testing artifact not a workflow bug - and for real in CI, where the diff correctly resolved to zero changed `.sv` files for this PR and both tool substeps were skipped as designed). Also found and fixed: the container job's user doesn't own the `actions/checkout`'d tree, so git (and bender, via git) refuses to touch it ("detected dubious ownership") - fixed with `git config --global --add safe.directory '*'` as each bender-using job's first step, confirmed necessary via a local repro of the same failure mode. Second push (commit `20e0b81`, run `33557013253`): `lint` green.
- [x] 2.2 **[eda]** Implement `sw`: `make ig-sw-all` inside the container (same bender-checkout host-workaround noted in `verilator-sim-flow`'s tasks.md if run on this dev machine; not expected to matter on a GitHub-hosted Linux runner). Non-zero exit on build failure. Verify locally: clean build exits 0; a deliberately broken `sw/tests/*.c` file exits non-zero with the compiler error visible.
      Confirmed on the GitHub-hosted Linux runner (no virtiofs bug there, as expected): `make ig-sw-all` completed successfully in both real CI runs (`sw` job green both times, runs `33556375472` and `33557013253`), including its own bender checkout + `git submodule update` running cleanly inside the container without the local-dev-only corruption seen while testing `verilator-sim-flow`. Deliberate-breakage half of this task's verify (introducing a broken `sw/tests/*.c` file) not separately exercised - `sw/tests/` lives in the vendored Cheshire checkout, not this repo, so there was nothing in-repo to break for that check; the build-failure path is implicit (a nonzero `make` exit already fails the step, same mechanism the real `bender sources`/verilator findings above exercised).

## 3. Stub jobs

- [x] 3.1 **[edit]** Implement `sim-unit` stub: check for the (not-yet-existing) coprocessor RTL/testbench path; if absent, print "stub: pending Phase 7 coprocessor RTL (docs/infra-plan.md Phase 7)" and exit 0; if present, run the real unit-test target instead (leave a clear `# TODO once Phase 7 lands:` comment naming what that target should be, since it doesn't exist yet). Verify: job runs on a PR today, exits 0, and the stub message is visible in the job log.
      One deviation from the literal draft, required by the spec's own "SHALL exit 0 regardless" wording (spec.md, "Stubbed jobs run, self-report their blocker, and never gate merges"): if `hw/coproc` *does* appear before this job's body is rewritten, it prints a `::warning::` annotation (visible, distinct, but non-failing) rather than erroring - an outright `exit 1` in that branch would violate the unconditional-exit-0 requirement while the job is still the stub implementation. Verified both real CI runs: `hw/coproc` doesn't exist on this branch, so the job printed "stub: pending Phase 7 coprocessor RTL..." and exited 0 both times (runs `33556375472`, `33557013253`).
- [x] 3.2 **[eda]** Implement `sim-soc` stub: run `make ig-sim-verilator` for real (against `sw/tests/helloworld.spm.elf`, `BOOTMODE=0 PRELMODE=0`) every time; if it exits non-zero, print "stub: pending verilator-sim-flow JTAG-DM blocker (task 3.1) — ran for real, did not pass, not gating yet" and exit 0 regardless; if it exits 0, print "green light passing — ready to graduate this job, see design.md D3" and still exit 0 (graduation is a deliberate follow-up per the spec, not automatic). Verify: job runs on a PR today, exits 0, and the log clearly states which branch it took.
      One refinement made during implementation, not a scope change: rather than presuming *why* `make ig-sim-verilator` failed (the draft's wording leaned on "the tracked JTAG-DM blocker" specifically), the actual message stays agnostic about the cause and just names it as the likely one among a few - because on `feat/ci-fast-lane` (branched from `main`, not from the still-open `verilator-sim-flow` PR) the target doesn't exist at all yet, so the real failure mode observed in both CI runs was `make: *** No rule to make target 'ig-sim-verilator'` (exit 2), not a JTAG-DM failure specifically. Verified for real, both runs: attempted the full real sequence (bender checkout, submodules, then the `make` call), got the "No rule to make target" failure, printed "stub: make ig-sim-verilator did not pass (exit 2) - see the log above for why. Likely the tracked verilator-sim-flow JTAG-DM blocker..., but could also mean that change hasn't merged yet, or another regression - not gating yet either way", and exited 0 (job green both times, runs `33556375472`, `33557013253`). Once `verilator-sim-flow` merges, this job will start actually reaching the tracked JTAG-DM blocker instead - message stays accurate either way.
- [x] 3.3 **[edit]** Implement `synth-coproc` stub: same pattern as 3.1 (check for a coprocessor module path, stub-and-exit-0 if absent). Verify: job runs on a PR today, exits 0, stub message visible.
      Same `::warning::`-not-`exit 1` treatment as 3.1, for the same spec reason. Verified both real CI runs: exited 0 with the stub message.

## 4. Branch protection

- [x] 4.1 **[gh]** Add `lint` and `sw` to `main`'s required status checks (`gh api repos/{owner}/{repo}/branches/main/protection` or the GitHub Settings UI if the token lacks admin scope — document whichever path was actually used). Verify: `gh api repos/{owner}/{repo}/branches/main/protection/required_status_checks` shows `contexts` containing exactly `lint` and `sw`, not the three stub job names.
      `gh api -X PATCH repos/wortexx/newt/branches/main/protection/required_status_checks -F strict=true -f 'contexts[]=lint' -f 'contexts[]=sw'` - the `gh` token already had sufficient admin scope, no Settings-UI fallback needed. Verified via the same endpoint (GET): `{"strict":true,"contexts":["lint","sw"],...}` - exactly `lint` and `sw`, none of the three stub job names.
- [x] 4.2 **[edit]** Update `docs/infra-plan.md` Phase 0's branch-protection line and Phase 3 section: check off delivered items, note which jobs are real vs. stubbed and why, record the exact required-status-checks list. Verify: doc reflects reality post-implementation.
      Phase 0's branch-protection line now states the required checks by name (`lint`, `sw`). Phase 3 gained a status block below its job table recording what's real/gating vs. stubbed/non-gating and why, plus a pointer to this change's planning record. Also corrected the pre-existing `bender check` inaccuracy in the Phase 3 table itself (see 2.1).

## 5. Documentation & closeout

- [x] 5.1 **[edit]** Add a short note to the top-level README or `.github/workflows/ci.yml`'s header comment explaining the stub-job convention (why a green `sim-soc`/`sim-unit`/`synth-coproc` doesn't mean what it will once Phase 7/the Verilator blocker land), so a future reader of a green PR isn't misled. Verify: comment present and accurate.
      Done via `ci.yml`'s own header comment (written as part of 1.1) rather than a separate README addition - the task explicitly allows either. Confirmed still accurate after the `bender check`→`bender sources` fix (header comment didn't reference that detail, so nothing to update there).
