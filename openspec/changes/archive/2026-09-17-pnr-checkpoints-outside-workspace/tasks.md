# Tasks — pnr-checkpoints-outside-workspace

Ordering follows design.md's Migration Plan: the workflow edit and its local rehearsal first (no VM), then docs, then the PR, then the real-run verification that rides on `post-merge-ci-verification`'s Run B. Everything unmarked is plain editing or read-only checks the agent does. Tasks marked **[VM/EDA]** start the Azure VM and are **user-triggered** (that change's design D8); none of them are required for this change's own completion gate, which is the local rehearsal in §1.

## 1. Workflow edit (inert until a run executes it — validated locally, no VM)

- [x] 1.1 Add the export path to `.github/workflows/pnr.yml` (design D1/D2/D3): workflow-level env `PNR_EXPORT_HOST_DIR: /home/newt/pnr-export` with a comment mirroring `PNR_RESTORE_HOST_DIR`'s; a third `volumes:` entry on the `pnr` job (`/home/newt/pnr-export:/pnr-export`) and job env `PNR_EXPORT_DIR: /pnr-export`; a new step "Export checkpoints out of the shared workspace" with `if: always()` placed immediately after "Place and route" that prunes everything under `$PNR_EXPORT_DIR`, creates `$PNR_EXPORT_DIR/$GITHUB_RUN_ID`, moves `target/ihp13/openroad/save/*.zip` and `save/synth-key.txt` there (leaving `pnr_status.log` and `reports/` in place), lists each exported file with its size plus the total and elapsed time, and ends with `chmod a+rwx "$PNR_EXPORT_DIR"` and `chmod -R a+rwX` on the run directory. The step's comment records the 2026-09-15 incident (runs 34783899813 / 34820756433) as the reason. Verify: `actionlint .github/workflows/pnr.yml` passes; `git diff -- .github/workflows/pnr.yml | grep -E '^[+-].*(if:|needs:|runs-on:|permissions:)'` shows only the new step's own `if: always()` line; the "Stage-status summary" and "Upload reports and logs" steps still reference `target/ihp13/openroad/save/pnr_status.log` unchanged.
  - Done 2026-09-16. Three edits to `pnr.yml`: workflow env `PNR_EXPORT_HOST_DIR: /home/newt/pnr-export` (after `PNR_RESTORE_HOST_DIR`, comment naming run 34783899813); `pnr` job gained the `/home/newt/pnr-export:/pnr-export` volume and `PNR_EXPORT_DIR: /pnr-export`; new step "Export checkpoints out of the shared workspace" (`if: always()`) between "Place and route" and "Stage-status summary". `actionlint .github/workflows/pnr.yml` exit 0, no output. `git diff -- .github/workflows/pnr.yml | grep -E '^[+-].*(if:|needs:|runs-on:|permissions:)'` printed exactly one line, `+        if: always()` — the new step's own. `grep -n 'save/pnr_status.log'` still shows both consumers (the summary step's `STATUS_LOG=` and the reports artifact path), unchanged.
- [x] 1.2 Rewrite `upload-checkpoints`' "Upload checkpoints" step and its job header comment (design D4): source is `${PNR_EXPORT_HOST_DIR}/${GITHUB_RUN_ID}`; branch on directory-missing (`::warning`, exit 0), no-zip-and-`needs.pnr.result != 'success'` (info, exit 0), no-zip-and-`needs.pnr.result == 'success'` (`::error` naming the directory, exit 1), and zips-present (existing `upload-batch` + `synth-key.txt` marker commands, unchanged flags, then `rm -rf` the directory with a `::warning` rather than a failure if the removal fails). Delete the "pnr job may have failed before floorplan" text and the header sentence justifying reading the workspace. Verify: `actionlint` passes; `grep -n 'GITHUB_WORKSPACE\|may have failed before floorplan' .github/workflows/pnr.yml` returns nothing in the `upload-checkpoints` job; `grep -n 'needs.pnr.result' .github/workflows/pnr.yml` shows the two branches; the `az storage blob upload-batch`/`upload` invocations differ from `main` only in `--source`/`--file` paths.
  - Done 2026-09-16. Step rewritten with all four D4 branches and the header's workspace justification replaced by the export-directory contract. `actionlint` exit 0. `grep -n 'GITHUB_WORKSPACE\|may have failed before floorplan'` returns one hit, line 561 — inside the `pnr` job's export-step comment, which quotes the old misleading message as history; nothing in the `upload-checkpoints` job, as the verify text requires. `git diff` shows the `az` invocations changed on exactly two lines: `--source "$SAVE_DIR"` → `"$SRC_DIR"` and `--file "$SAVE_DIR/synth-key.txt"` → `"$SRC_DIR/synth-key.txt"`; every other flag is byte-identical to `main`.
  - **Deviation from this task's verify text, deliberate.** `needs.pnr.result` is bound once to a step-level env var (`PNR_JOB_RESULT`, line 715) instead of being interpolated into the shell body twice, so `grep -n 'needs.pnr.result'` shows one line, not two branches; the two branches read `$PNR_JOB_RESULT` at lines 734 (`= "success"` → `::error`, exit 1) and 738 (info, exit 0). Two reasons: `.claude/rules/github-actions-ci-cd-best-practices.md` calls for keeping `${{ }}` out of `run:` bodies, and the env binding is what makes the body runnable verbatim in task 1.3's rehearsal (no expression substitution needed to simulate a result). The specified behaviour — two branches keyed on whether the `pnr` job succeeded — is implemented in full.
- [x] 1.3 Local rehearsal of both step bodies (design D5, the completion gate — no EDA tools): extract the two `run:` bodies verbatim from `pnr.yml` (e.g. `python3 -c 'import yaml…'` or `yq`) into the scratchpad, and run them under `bash -euo pipefail` against a scratch tree with `GITHUB_WORKSPACE`/`GITHUB_RUN_ID` set, `PNR_EXPORT_DIR`/`PNR_EXPORT_HOST_DIR` pointing at a scratch export root pre-seeded with a stale `<other_run_id>/` directory, `save/` holding zips named for every stage through `grt` plus `synth-key.txt` and `pnr_status.log`, and a stub `az` on `PATH` that logs its arguments. Verify, quoting output here: after the export body, `save/` contains exactly `pnr_status.log`, the stale directory is gone, the run directory holds the 6 zips + `synth-key.txt` and `stat -c %a` on it shows group/other write; then the upload body (a) with the run directory removed prints the `::warning` and exits 0, (b) with an empty run directory and a simulated `needs.pnr.result` of `failure` prints the info line and exits 0, (c) with an empty run directory and `success` prints the `::error` and exits 1, (d) with the seeded run directory the stub `az` logs one `upload-batch` whose `--source` is the run directory and one `upload` whose `--file` ends in `synth-key.txt`, and the run directory no longer exists afterwards.
  - Done 2026-09-16. Harness, extractor and both extracted bodies live in the session scratchpad (`extract_step.py`, `export_body.sh`, `upload_body.sh`, `harness.sh`). The extractor reimplements a `|` block scalar (take following lines indented deeper than the `run:` key, strip the block indent) because no YAML library is installed here. **The export body needed no substitution at all — it contains zero `${{ }}` expressions.** The upload body contains five, all inside `az` flag values; the harness substitutes them (`AZURE_STORAGE_ACCOUNT`→`newtpnrckpt`, `AZURE_CHECKPOINT_CONTAINER`→`pnr-checkpoints`, `github.run_id`→the run id) exactly as the runner does before handing the body to bash, leaving 0 expressions. Both ran under `bash -euo pipefail` (matching GitHub's `bash --noprofile --norc -eo pipefail`) with GNU find/stat first on `PATH` — `brew install findutils` was needed, since the bodies use GNU `find -printf` (new) and `-quit` (pre-existing) and macOS ships BSD find.
  - Export body, seeded with 6 zips (`power_grid pre_place gpl2 dpl cts grt`) + `synth-key.txt` + `pnr_status.log` and a stale `11111111/` in the export root:
    ```
    Exporting checkpoints from target/ihp13/openroad/save (56K) to .../pnr-export/99999999
    Exported 6 checkpoint(s) in 0s, 52K total:
      basilisk.cts.zip (8192 bytes)
      ... basilisk.power_grid.zip, basilisk.pre_place.zip, basilisk.dpl.zip, basilisk.gpl2.zip, basilisk.grt.zip ...
      synth-key.txt (39 bytes)
    ```
    `save/` afterwards holds exactly `pnr_status.log`; `reports/` untouched; the stale `11111111/` is gone (export root holds only `99999999`); `stat -c %a` gives `777` on both the export root and the run directory and `666` on each file, so the runner user can read them and remove the directory.
  - Upload body, all four D4 branches: **(a)** run dir missing, `pnr=cancelled` → `::warning::... did not reach its checkpoint-export step ...`, exit 0, no `az` calls. **(b)** empty run dir, `pnr=failure` → `No checkpoint .zip files in ... failed before floorplan saved one.`, exit 0, no `az` calls. **(c)** empty run dir, `pnr=success` → `::error::the pnr job succeeded but ... holds no checkpoint .zip files ...`, **exit 1**, no `az` calls. **(d)** seeded run dir, `pnr=success` → exit 0 and exactly two stub-`az` calls, `storage blob upload-batch ... --destination pnr-checkpoints/99999999 --source <run dir> --pattern *.zip` and `storage blob upload ... --name 99999999/synth-key.txt --file <run dir>/synth-key.txt --overwrite`; the run directory is gone afterwards and the export root is empty.
  - Extra check beyond the task text, covering the branch-1-vs-3 distinction end to end: run the export body against a `save/` holding no zips at all (flow died before floorplan). It logs `No checkpoint .zip files were produced ...`, exits 0, and **still creates the empty run directory** — feeding that to the upload body with `pnr=success` produces branch (c)'s `::error` and exit 1 rather than branch (a)'s warning, which is exactly the distinction the design relies on.
  - Not covered locally, by design: the cross-user part (container root writes, host `newt` reads and removes) and the EXDEV copy-then-unlink cost of a real ~20GB move. Task 4.1's Run B log is the first real check of both.

## 2. Documentation truth-maintenance

- [x] 2.1 Update `docs/pnr-pipeline.md`: the `pnr` row mentions the export step, the `upload-checkpoints` row says it uploads from `/home/newt/pnr-export/<run_id>` and fails when a successful flow left nothing; the `concurrency` paragraph no longer says the workspace hazard is only manual work — it says the lanes' automated checkouts are now harmless to a pending upload and manual work is the remaining hazard; add a Notes entry recording the 2026-09-15 finding (Run A 34783899813, synth 34820756433, the 6 s window, the 2.5 h gap, the misleading message) and the fix. Verify: `grep -n 'pnr-export' docs/pnr-pipeline.md` hits the job table and the Notes entry; `grep -n 'does \*\*not\*\* protect against manual' docs/pnr-pipeline.md` returns nothing.
  - Done 2026-09-16. `grep -n 'pnr-export' docs/pnr-pipeline.md` hits four lines: the `pnr` row (14), the `upload-checkpoints` row (15), the rewritten `concurrency` paragraph (24) and the new Notes entry (125). The stale claim greps clean (exit 1). The `concurrency` paragraph now says the group only serializes `pnr.yml` runs against each other, that the lanes' automated checkouts can no longer hurt each other because the `pnr` job hands its checkpoints off before releasing the runner, and that manual out-of-band work is the remaining sharp edge. The Notes entry leads the section and carries the full timeline (22:02:34Z pnr end, 22:02:40Z the synth checkout's clean, 00:31:40Z the upload job, the "may have failed before floorplan" message) and names this change as the fix.
- [x] 2.2 Add a one-line comment above `synth.yml`'s `actions/checkout` step stating that its clean wipes the shared workspace and that `pnr.yml` no longer keeps anything there between jobs (pointing at `pnr.yml`'s export step). Verify: `actionlint .github/workflows/synth.yml` passes and `git diff -- .github/workflows/synth.yml` contains only `#` lines.
  - Done 2026-09-16. Nine-line comment above the `synth` job's `actions/checkout`, naming what its clean wipes, why that is now safe (pnr.yml's export step), the run it destroyed (34783899813), and the manual-work edge that remains. `actionlint .github/workflows/synth.yml` exit 0. Every added line in `git diff -- .github/workflows/synth.yml` starts with `#` (the non-comment filter returns nothing); no behaviour of the synth lane changed.
- [x] 2.3 Update `docs/infra-plan.md`: add the incident and its fix to Phase 5's History paragraph (with run IDs and date), and replace the follow-up `post-merge-ci-verification` task 2.1 parked for this change with a pointer to this change's name; do not tick any Phase 9 box (those belong to that change). Verify: `grep -n 'pnr-checkpoints-outside-workspace' docs/infra-plan.md` hits; `git diff -- docs/infra-plan.md | grep -E '^\+.*\[x\]'` is empty.
  - Done 2026-09-16. Phase 5 gained a "Checkpoint loss, 2026-09-15 (fixed)" paragraph with both run IDs, the 2s/6s/2.5h timeline, why neither guard covered it, and the fix. `grep -n 'pnr-checkpoints-outside-workspace'` hits lines 243 and 346; the new-`[x]` filter returns nothing, so no Phase 9 box was touched.
  - **Finding worth flagging:** the follow-up that `post-merge-ci-verification` task 2.1 said it had "recorded for a future change" was never actually written into this file — that change's task 6.4, which was to add it, is still open. So there was nothing here to replace. I added it to Phase 6's follow-ups list as a plain bullet (not a checkbox, keeping the no-new-`[x]` verify honest) marked *fixed, not open*, pointing at this change and noting that 6.4 expects the list to carry it. When that task comes up it will find the entry already there.

## 3. PR

- [x] 3.1 Open the PR for §1–§2 (title `ci(pnr): export checkpoints outside the shared runner workspace before upload`), body linking `post-merge-ci-verification` task 2.1's finding, and merge once `lint`/`sw` are green. Verify: `gh pr checks <n>` shows `lint`/`sw` success and no `pnr.yml` or `synth.yml` run was triggered by the PR (`gh run list --workflow=pnr.yml --limit 3` shows no new run for the PR's SHA); after merge, `gh workflow view pnr.yml --yaml | grep -c 'pnr-export'` is ≥ 3 (env, volume, step). Record the PR number and merge commit here.
  - **In progress 2026-09-16 — PR open and green, merge is the user's.** Branch `ci/pnr-export-checkpoints` off `main`, commit `15076a9`, [PR #25](https://github.com/wortexx/newt/pull/25). All five checks pass: `lint` 1m44s, `sw` 3m38s, `sim-soc` 9m18s, `sim-unit` 4s, `synth-coproc` 7s (run 35069003579). No VM lane was triggered — the only workflow with a run at SHA `15076a9` is `CI`; the newest `pnr.yml` run is still the checkpoint-source dispatch 34938462965 and the newest `synth.yml` run is still the 2026-09-14 schedule.
  - `gh pr merge 25 --squash --delete-branch` was **blocked by this session's permission classifier**, so the user merged it, as with [PR #21](https://github.com/wortexx/newt/pull/21) before it.
  - **Done 2026-09-16.** PR #25 merged at 2026-09-16T08:05:38Z as squash commit `0b66a9c807562cfe0452af2b1d8966692798fd32` (`ci(pnr): export checkpoints outside the shared runner workspace before upload (#25)`), now the tip of `main`. Post-merge check on the default branch: `gh workflow view pnr.yml --yaml | grep -c 'pnr-export'` = **4** (≥ 3 as required) — `PNR_EXPORT_HOST_DIR` at line 98, the `/home/newt/pnr-export:/pnr-export` volume at 230, `PNR_EXPORT_DIR` at 234, plus the export step's own prune comment at 587. The step itself (`- name: Export checkpoints out of the shared workspace`, line 576) and all four upload branches (`PNR_JOB_RESULT` at 715, `SRC_DIR` at 718, the three outcome messages at 725/734/738) are live. Merging triggered no VM lane, as expected — `pnr.yml`'s `push` trigger is `tags: ['**']` only, and the newest `pnr.yml` run is still the checkpoint-source dispatch 34938462965.

## 4. Real-run verification (rides on `post-merge-ci-verification`; not a gate for archiving this change, but record what is observed)

- [x] 4.1 **[VM/EDA]** When the user dispatches that change's Run B (its task 3.1) after 3.1 above is on `main`: read the `pnr` job's "Export checkpoints out of the shared workspace" step log and the `upload-checkpoints` log. Verify and quote here: the export step lists `basilisk.grt_repaired.zip` among the restored-plus-rerun set with sizes and an elapsed time; `upload-checkpoints` logs the upload from `/home/newt/pnr-export/<B>` and the directory removal; `az storage blob list --container-name pnr-checkpoints --prefix <B>/ -o table` (or that change's own 3.1 verification) shows the zips and `synth-key.txt`. Cost is Run B's own (≈ $0.35), nothing additional.
  - **Done 2026-09-16. Run B = 35100509927**, `workflow_dispatch` on `main` at SHA `0b66a9c` — the merge commit itself, so it ran the fixed workflow. Dispatched by the user 13:13:09Z, concluded **success** at 13:24:20Z (~11 min): `start` 13:13:14→13:15:23, `restore-checkpoints` 13:15:26→13:15:49, `pnr` 13:15:52→13:22:34, `upload-checkpoints` 13:22:36→13:22:58, `stop` 13:23:00→13:24:20. Inputs confirmed in the job env: `PNR_RESUME_FROM_RUN: 34938462965`, `PNR_RESUME_EXCLUDE: grt_repaired`, `PNR_STOP_AFTER=grt_repair`.
  - Resume path, as context for the export: `RESUME MODE: restoring checkpoints from run 34938462965`, `Staged 11 checkpoint(s)`, `Cache hit: restored 121M from /synth-cache/synth-16d997ae59b007066bccc5be41cad4f8`, `Netlist identity matches (synth-16d997ae59b007066bccc5be41cad4f8) - safe to resume.` (the guard's matching path), `excluded: basilisk.grt_repaired.zip (its stage will re-run)`, then `floorplan`…`grt` each `already has checkpoint ... - resuming past it`. `pnr_status.log`: `floorplan/pre_place/gpl/dpl/cts/grt skipped resume`, **`grt_repair ok skipped`**, **`grt_repair stop-after`** — exactly design D1's forecast.
  - **Export step output** (`Export checkpoints out of the shared workspace`, 13:22:18Z):
    ```
    Exporting checkpoints from target/ihp13/openroad/save (3.3G) to /pnr-export/35100509927
    Exported 11 checkpoint(s) in 0s, 1.5G total:
      basilisk.cts.zip (134341886 bytes)
      basilisk.dpl.zip (132661902 bytes)
      basilisk.floorplan.zip (83574933 bytes)
      basilisk.gpl.zip (115329546 bytes)
      basilisk.gpl2.zip (140342537 bytes)
      basilisk.gpl_fix.zip (115329570 bytes)
      basilisk.gpl_repaired.zip (127572140 bytes)
      basilisk.grt.zip (234810134 bytes)
      basilisk.grt_repaired.zip (234810188 bytes)
      basilisk.power_grid.zip (91795097 bytes)
      basilisk.pre_place.zip (95372989 bytes)
      synth-key.txt (39 bytes)
    ```
    `basilisk.grt_repaired.zip` is present as the task required, freshly written by the re-run stage — 54 bytes larger than the restored `basilisk.grt.zip`, consistent with `PNR_SKIP_GRT_REPAIR=1`'s skip path re-saving the `grt` checkpoint under the `grt_repaired` name.
  - **Upload job output**: `Uploading 11 checkpoint(s), 1.5G total, from /home/newt/pnr-export/35100509927` — reading from the export directory, not the workspace — then all 11 blobs uploaded and listed back by `upload-batch` as `https://newtpnrcheckpoints.blob.core.windows.net/pnr-checkpoints/35100509927/<name>.zip` (13:22:42→13:22:54). `PNR_JOB_RESULT: success` appears in the step env, so branch 4 was taken for the right reason. That output covers the task's `az storage blob list --prefix 35100509927/` check directly.
  - **The cross-user handoff works — the one thing the local rehearsal could not cover.** The container writes as root; `upload-checkpoints` runs on the host as `newt`. It read every file and the `rm -rf "$SRC_DIR"` succeeded: the only annotations in the entire run are `RESUME MODE` (expected), the missing-DEF warning (expected under `stop_after=grt_repair`) and two pre-existing Node 20 deprecation notices. **No `could not remove ... - the next run's export step will prune it` warning was emitted**, which is the negative evidence that design D3's chmod does its job.
  - **Finding — the move is far cheaper than design D2 and the Risks table assumed.** 1.5G moved `in 0s` (sub-second at `$SECONDS` granularity). D2 reasoned `mv` across the bind mount would degrade to copy-then-unlink on EXDEV, and Risks budgeted "minutes"; at this size it is not measurable. Even pessimistically extrapolated, a full ~20GB run costs seconds. No action needed — D1's rejected `SAVE=` zero-copy alternative stays rejected, now on measured rather than assumed grounds.
  - **Observation, not a regression.** `save/` measured 3.3G but only 1.5G moved: the balance is non-`.zip` stage scratch (checkpoint contents unpacked by `load_checkpoint`), which stays in the workspace for the next checkout to clean. Blob has only ever held `*.zip` plus the marker — the pre-change upload used the same `--pattern "*.zip"` — so nothing a resume needs was left behind. This run proves it, having resumed successfully from 34938462965's zip-only blobs.
- [x] 4.2 **[VM/EDA]** ~~Optional, only if the user wants the collision reproduced under the fix: combine with that change's task 3.3 — dispatch `synth.yml` while a slice run's `pnr` job is `in_progress` (needs `synth.yml` enabled, its task 5.3) and record which ordering GitHub picked. Verify: if synth ran first (ordering (1)), its `actions/checkout` timestamp falls between `pnr`'s end and `upload-checkpoints`' start **and** `upload-checkpoints` still uploaded every zip — the direct proof; if `upload-checkpoints` ran first (ordering (2)), note that this run observed the `stop` skip branch for 3.3 instead and the collision was not exercised. Either way, quote the three timestamps here.~~
  - **Not exercised as of 2026-09-16, and optional by its own terms.** Its precondition is still unmet: `gh api .../actions/workflows/synth.yml --jq .state` is `disabled_manually` (disabled on 2026-09-15 to stop the nightly cron reproducing the wipe during the unblock run). Re-enabling it is `post-merge-ci-verification` task 5.3, which no longer has to wait for an idle P&R lane — this change is what removed that constraint.
  - Run B did **not** exercise it: nothing else was on the runner, so `upload-checkpoints` started 2s after `pnr` ended (13:22:34 → 13:22:36) with no foreign checkout in between. The fix is therefore verified as *correct* (4.1) but not yet *under the original collision*. Whether to spend a synth run (~3h, ~$3.60) reproducing it is the user's call; 4.1's evidence stands either way, because the export completes before the `pnr` job releases the runner regardless of what claims it next.
  - **SKIPPED by user decision, 2026-09-16 — not performed.** Asked explicitly whether to archive now or hold this change open until `post-merge-ci-verification` task 3.3 provides a free ride-along; the user chose to archive now and skip this. Ticked only to close the list: **the collision was never reproduced under the fix.** Anyone wanting that evidence later should combine with that change's task 3.3 as this task describes — it needs no new run of its own, just `synth.yml` re-enabled (its task 5.3) and a slice run to overlap with.

## 5. Wrap-up

- [x] 5.1 `openspec validate pnr-checkpoints-outside-workspace --strict` passes and every task above carries its verification output; then hand off to `/opsx:archive` (which syncs the `ci-pipeline` delta into `openspec/specs/`). Verify: validate exits 0; `git status` clean after the archive commit.
  - **Done 2026-09-17.** `openspec validate pnr-checkpoints-outside-workspace --strict` exits 0 ("Change ... is valid"). Every task 1.1–4.1 carries its verification output; 4.2 is ticked as **skipped, not done** (see its note — the collision was never reproduced under the fix).
  - Archived via `/opsx:archive` to `openspec/changes/archive/2026-09-17-pnr-checkpoints-outside-workspace/`, `.openspec.yaml` preserved. The `ci-pipeline` delta was synced into `openspec/specs/ci-pipeline/spec.md` first, then verified to leave nothing to apply: both MODIFIED requirements now read byte-identical to the delta, all 20 main requirements intact, four additions and zero removals (the workspace paragraph and the "Synth job takes the runner between the P&R job and the checkpoint upload" scenario on the coexistence requirement; the "nothing was saved" vs "what was saved is gone" paragraph and the "Successful flow but no checkpoints to upload" scenario on the publish/preserve requirement).
