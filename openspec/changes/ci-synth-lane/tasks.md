## 1. Preflight verifications (cheap checks before anything is built on them)

- [x] 1.1 Verify GitHub larger-runner availability for `wortexx/newt` (`gh api /repos/wortexx/newt` → `owner.type`, plus a direct probe of `/repos/wortexx/newt/actions/hosted-runners`). **Result: unavailable** — `wortexx/newt` is a personal-account repo; GitHub's hosted larger-runners API is org-scoped only (`/orgs/{org}/actions/hosted-runners`), confirmed 404 on the repo-scoped probe. Per this task's own instruction, stopped and surfaced design.md D1's reroute options to the user rather than substituting silently. **User chose:** pull Phase 5/6's self-hosted Azure VM forward (option b). Design.md D1/D1a/D1b and this file updated accordingly; see tasks 1.1a–1.1d below for the concrete follow-on work this decision requires.
- [x] 1.1a **Done (2026-09-02), user-executed with agent verification/remediation.** Azure provisioning (`az provider register`, `az group create`, `az vm create`, NSG, auto-shutdown) was denied by the harness's permission classifier for agent execution — it blocked even the free, reversible `az provider register` step — so the user ran the runbook by hand. **Region corrected mid-runbook:** `westeurope` (the original default, matching this subscription's pre-existing unrelated resource groups) turned out `NotAvailableForSubscription` for every 16-vCPU candidate SKU checked; a live `az vm list-skus` sweep across five regions found `swedencentral`/`uksouth` fully available, runbook moved to `swedencentral`. **Auto-shutdown time corrected** from an initial 2200 UTC to 1000 UTC (design D1a: 2200 would have killed the VM hours before a 02:30 UTC nightly cron run could finish). Verified directly (`az vm show -d`): `newt-synth-runner` in `newt-synth-lane-rg`/`swedencentral`, `Standard_E16ds_v5`, Ubuntu 24.04 LTS (`24.04.202608270`), `disablePasswordAuthentication: true`, 128GB Premium_LRS OS disk, `powerState: VM running`, public IP `4.165.136.61`. **Found and fixed two gaps by checking live state rather than trusting the runbook was followed as written:** (1) `az vm create`'s own default behavior had added an unscoped `default-allow-ssh` NSG rule (priority 1000, source `*`) alongside the intended `AllowSSHFromMe` rule (priority 100, source-scoped) — since NSG rules fall through by priority, this left SSH genuinely open to the whole internet despite the scoped rule being present; the runbook never told the user to remove it. Deleted (`az network nsg rule delete ... default-allow-ssh`); re-verified only `AllowSSHFromMe` (source `193.0.219.126/32`) remains. (2) auto-shutdown was still at the pre-correction 2200 UTC (an earlier runbook version had been run); reset to 1000 UTC and re-verified (`dailyRecurrence.time: "1000"`, `timeZoneId: UTC`).
- [x] 1.1b **Done (2026-09-02), agent-executed via direct SSH** — the VM's registered SSH key (`~/.ssh/id_rsa`, matched against the key `az vm show` reported on the VM) was reachable from this session, so this task ran directly rather than via a handoff. Docker installed via the official convenience script (`get.docker.com`), `newt` added to the `docker` group, `docker run hello-world` verified. Runner registration token fetched via `gh api repos/wortexx/newt/actions/runners/registration-token -X POST`; latest runner release resolved dynamically via `gh api repos/actions/runner/releases/latest` (v2.337.0 at the time) rather than a hardcoded version, downloaded/extracted/configured on the VM with label `self-hosted-synth` and name `newt-synth-runner`. Installed as a systemd service (`./svc.sh install && ./svc.sh start`) — confirmed `enabled` (survives reboot) and `active (running)`, log shows "Connected to GitHub" / "Listening for Jobs". Verified via `gh api repos/wortexx/newt/actions/runners`: `{"name":"newt-synth-runner","status":"online","labels":["self-hosted","Linux","X64","self-hosted-synth"]}`. Also confirmed the systemd service's `User=newt` carries the `docker` group (`988(docker)` in its group list) so job containers will have Docker access.
- [x] 1.1c **Done (2026-09-02).** Pulled real pricing via the Azure retail-prices API (not design.md's rough calculator estimate): `Standard_E16ds_v5`/`swedencentral` on-demand compute is **$1.216/hour** (~$888/mo if always-on) — notably higher than design.md's ~$500–700/mo guess — plus a flat ~$26/mo for the 128GB OS disk and static public IP that continue billing even while stopped. Spot pricing exists (~$0.225/hr, ~80% cheaper) but needs eviction-handling logic that's genuinely Phase 6 scope, not set up now. Presented these numbers to the user; **decision: manual stop/start discipline, with the already-configured 10:00 UTC auto-shutdown (task 1.1a) as the backstop** — not always-on. Full numbers and the decision to be folded into `docs/infra-plan.md` at task 4.1 closeout.
- [x] 1.1d **Done (2026-09-02), user-executed via UI.** No REST API exposes this setting for an individual (non-org) repo — checked `repos/{owner}/{repo}/actions/permissions/workflow` and `.../actions/permissions`, neither has it; confirmed UI-only here. GitHub has since relabeled the strictest tier "Require approval for all external contributors" (was "...outside collaborators" per the original design note) — same setting, same effect. User enabled and saved it via Settings → Actions → General. Not independently re-verified via API (none exists); this is layer 1 of D1b's two-layer gate, with layer 2 (the workflow-level `head.repo.full_name` check) still to land in task 3.1 as the belt-and-suspenders backup regardless of this UI setting's state.
- [x] 1.2 In a local `newt-eda:dev` container, verify STA tooling per design D4. **Result: `sta` (standalone OpenSTA) is present at `/build/bin/sta`** — the original "might be missing" concern (design D4) was wrong; `run-sta`'s default `STA ?= sta` needs no override. Verified: (a) `command -v sta` resolves in-container; (b) `target/ihp13/openroad/src/basilisk.sdc` exists at the relative path `opensta_timings.tcl` expects from cwd `target/ihp13/yosys`; (c) ran `sta` against a throwaway tiny netlist+liberty+SDC to confirm the real `report_checks` output format (`slack (MET)`/`slack (VIOLATED)` lines) that `synth_metrics.py` (task 2.1) parses. The real full-chip invocation is `make -C target/ihp13/yosys -f yosys.mk run-sta` (no env override) — recorded for task 3.1. Design.md D4 updated to match.

## 2. Metrics script and baseline

- [x] 2.1 Write `target/ihp13/yosys/scripts/synth_metrics.py`: parse cell count + chip area from `basilisk_area.rpt`, DFF count (sum of `sg13g2_df*` rows, matching the Phase 1 gate's counting method), worst slack across path groups from the STA output (tolerating its absence per D4), and `CHECK` problem count from `basilisk_synth.rpt`; emit a Markdown metric/baseline/current/delta table to `$GITHUB_STEP_SUMMARY` (stdout fallback); exit non-zero iff CHECK problems > 0 or a required report is missing/unparseable. **Verification note (deviates from the literal task): the Phase 1 gate's `reports/` were not actually kept on disk** — gitignored and cleaned up after that run (design D5 corrected). Verified instead against fixture reports built to match the real report format, which was captured from an actual `yosys stat`/`check` and `sta report_checks` run in-container against a throwaway design (same session, see task 1.2). Table renders correctly, exit code 0, near-zero deltas when fixture values equal the baseline. Real-gate-data validation happens at task 3.3's actual run.
- [x] 2.2 Add `target/ihp13/yosys/synth-baseline.json` seeded with the Phase 1 gate numbers (714166 cells, 17156844.69 µm², 89258 DFFs, wns_ps ≈ −2500) plus provenance fields (source run, date, image tag). Verified: `synth_metrics.py` loads it and reports exactly-zero deltas against matching fixture reports. **License note (corrects the propose-stage assumption):** this directory's existing scripts (`delays_from_log.py`, `yosys.mk`, `pickle.mk`) all use Apache-2.0, not Solderpad — followed that established convention, not the general "Solderpad for hardware/scripts" guidance, since this file lives alongside and matches those siblings exactly.
- [x] 2.3 Negative-path check of the script: against a doctored copy of `basilisk_synth.rpt` with a nonzero problem count → exit 1, error logged, summary still emitted for diagnosis; against a missing `basilisk_area.rpt` → exit 1, error logged, no summary (fails before parsing); against a missing STA report only → exit 0, "WNS unavailable" row, warning logged but non-fatal. All three verified directly.

## 3. Workflow

- [x] 3.1 **Done (2026-09-02).** Wrote `.github/workflows/synth.yml` per design D2/D3/D6/D7: triggers (cron 02:30 UTC, `workflow_dispatch`, `pull_request` [labeled, synchronize, reopened] guarded by both the `full-synth` label check and `head.repo.full_name == github.repository` per D1b layer 2), `runs-on: [self-hosted, self-hosted-synth]`, `newt-eda:dev` container, bash default shell, `safe.directory` step, per-ref concurrency with cancel-in-progress, `timeout-minutes: 350`; steps: retry-wrapped `make ig-hw-all` → `make pickle-all` → `make synth-all` → STA → `synth_metrics.py`; netlist artifact on success, reports+logs `if: always()`, both 30-day retention; header comment covers the lane's non-gating contract, the manual-VM-lifecycle caveat (including that the nightly cron only produces a run on nights the VM happens to be up — a known gap until Phase 6 automates lifecycle, not a workflow bug), and the fork-PR gate. **Real bug caught while writing this, not during a later test run:** the standalone `make -C target/ihp13/yosys -f yosys.mk run-sta` step doesn't go through `iguana.mk`, so it wouldn't inherit `RTL_NAME=basilisk` and would silently look for the wrong netlist (`iguana_chip.*` instead of `basilisk.*`) — fixed by passing `RTL_NAME=basilisk` explicitly on that step; design.md D4 and task 1.2's note corrected to reflect it (task 1.2's original "no override needed" verification only checked the `STA=` binary path, not this separate variable). Added `.github/actionlint.yaml` declaring the `self-hosted-synth` custom label (actionlint doesn't know custom self-hosted labels by default — false-positive otherwise). Verified: installed `actionlint` locally (`brew install actionlint`), `actionlint .github/workflows/synth.yml` → exit 0, no findings. (`ci.yml` has one pre-existing, unrelated shellcheck info-note — out of scope for this change.)
- [x] 3.2 **Done (2026-09-02), with one honest gap noted below.** Required merging PR #11 first — GitHub only recognizes `pull_request`/`workflow_dispatch` triggers for a workflow file once it exists on the default branch; a brand-new workflow on a feature branch doesn't fire yet (a real discovery, not assumed going in). Opened scratch PR #12 from branch `scratch-trigger-test`: unlabeled → confirmed zero runs on `synth.yml` (queried `.../actions/workflows/synth.yml/runs`, empty). Added the `full-synth` label (created it first — didn't exist on the repo yet) → a run started immediately (`event: pull_request`, `head_branch: scratch-trigger-test`), confirmed actually scheduled onto `newt-synth-runner` and past "Set up job" before cancelling (didn't let the ~3h flow complete twice — task 3.3 covers the real end-to-end run). Pushed a second commit to the same labeled PR → a new run started for the new `head_sha`, confirming the re-run-on-push scenario; cancelled after confirming scheduling. Closed PR #12 and deleted the scratch branch without merging. **Gap: the fork-PR scenario was not live-tested** — this GitHub account has no second identity to open a genuine fork PR from. Verified instead by code inspection: the workflow's `if:` condition (`head.repo.full_name == github.repository`) is straightforward boolean logic that evaluates false for any fork (its `head.repo.full_name` differs from `wortexx/newt`), and task 1.1d's repo-level "require approval for all external contributors" setting is a second, independent layer that doesn't depend on this condition being correct. Flagging this as inspected-not-executed rather than claiming a live test that didn't happen.
- [x] 3.3 **Done (2026-09-02), after fixing two real bugs the first real run surfaced.** First `workflow_dispatch` run (on `main` post-merge) completed all 14 steps successfully in ~2h35m (06:46→09:22 UTC) — the flow itself, netlist upload, and reports upload all genuinely work. But per design's own risk note ("do not merge a lane whose first datapoint is unexplained"), investigated the actual numbers rather than taking a green checkmark at face value:
  - Downloaded the real `synth-reports` artifact and ran `synth_metrics.py` locally against it (more thorough than trusting the CI job's own summary blindly). Found chip area off by −93% and DFF count off by +100% vs. baseline, with cell count matching exactly — a red flag pattern (identical cell count but wildly different area/DFF strongly suggested a parsing bug, not a real synthesis difference, since cell library area/reuse doesn't change independent of cell count).
  - **Root cause:** on this design (regular, hierarchical, unlike the earlier flat single-module fixtures), yosys `stat` prints one `=== <submodule> ===` block per submodule *before* the final `=== design hierarchy ===` rollup with the true top-level totals. `synth_metrics.py` scanned the whole file, double/many-times-counting cells that appear in both their own submodule's block and the rollup, and its area regex only matched the submodule wording ("Chip area for module") not the rollup's actual wording ("Chip area for **top** module"), so it silently picked up the last submodule's number instead. Fixed by scoping cell/area/DFF parsing to text at-or-after the last `=== design hierarchy ===` marker (falling back to whole-file scanning when that marker is absent, preserving the flat-design case) and matching "top module" specifically when present. Re-verified against both the real report (now: cell count, chip area, DFF count all ~exactly match baseline - genuine near-zero drift, confirming the flow itself is healthy) and the original fixtures (unchanged result - no regression).
  - **Second bug, in the workflow itself:** the real run's `sta_output.txt` was empty except for make's own directory-enter/leave lines - `run-sta` had actually failed with `make: *** No rule to make target '/basilisk.sv2v.v'`. Root cause: `PICKLE_OUT` (only ever defined in `pickle.mk`, which this standalone `make -f yosys.mk` invocation never includes) resolved to empty, so `SV2V_FILE := $(PICKLE_OUT)/$(RTL_NAME).sv2v.v` - one of `run-sta`'s prerequisites via `$(NETLIST)` - became the bogus path `/basilisk.sv2v.v`. Task 1.2's in-container STA check never caught this because it pointed `read_sdc` at a file in the same directory rather than exercising the real relative-path prerequisite chain. Fixed by additionally passing `PICKLE_OUT=../pickle/out` (relative to the `-C target/ihp13/yosys` cwd) alongside `RTL_NAME=basilisk`. Verified via `make ... -n` (dry-run) locally against a synthetic pickle-output layout: no more "No rule" error, correctly resolves to invoking `sta` with the right netlist path; also independently confirmed the relative SDC path (`../openroad/src/basilisk.sdc`) resolves correctly from that same cwd.
  - Both fixes committed (PR #13, merged) and re-verified via a second `workflow_dispatch` run. That run hit a real operational gotcha unrelated to either fix: dispatched at 09:36 UTC, it ran cleanly through `ig-hw-all`/`pickle-all` and was 24 minutes into `synth-all` when the VM's 10:00 UTC auto-shutdown (task 1.1a/D1a) deallocated it out from under the job (step showed `cancelled`, VM came back `VM deallocated`) - see design.md D1a's new note on this gap (the auto-shutdown's buffer math only protected the nightly cron's fixed start time, not an arbitrarily-timed dispatch). Restarted the VM (`az vm start`; runner reconnected on its own via the systemd service, confirming task 1.1b's setup holds) and re-dispatched at 10:08 UTC, safely within a day of the next auto-shutdown.
  - That third run completed all 14 steps successfully (~2h32m, 10:08→12:40 UTC) - metrics parsing bug confirmed fixed (`synth_metrics.py` re-run against the downloaded report shows exact-match cell count and chip area, DFF count matching to the last digit vs baseline), but investigating the STA output (not just trusting the green step) surfaced a **third real bug**: `sta_output.txt` showed the `sta` binary actually running this time (PICKLE_OUT fix confirmed working) but erroring with `couldn't read file "src/basilisk_instances.sdc": no such file or directory`. Root cause: `basilisk.sdc`'s own `source src/basilisk_instances.sdc` line was written assuming cwd `target/ihp13/openroad` (confirmed via `chip.tcl`'s real invocation: `read_sdc src/basilisk.sdc`, run from openroad.mk's own directory) - directly incompatible with `opensta_timings.tcl`'s own requirement of cwd `target/ihp13/yosys`. Two flow scripts with mutually exclusive cwd assumptions for the same file; nothing in this repo appears to have exercised `run-sta` standalone before this change to have caught it, and task 1.2's own STA verification only checked file *existence* at the expected path, never actually read the real `basilisk.sdc`/`basilisk_instances.sdc` chain end-to-end - a real gap in that earlier verification. Fixed with a workflow-only symlink (`target/ihp13/yosys/src/basilisk_instances.sdc -> ../../openroad/src/basilisk_instances.sdc`, created at runtime, never committed) rather than editing either flow script, staying inside the proposal's "flow invoked as-is" boundary. Verified locally: the symlink resolves correctly from cwd `target/ihp13/yosys` and successfully reads the real file's content; `actionlint` still clean after the workflow update.
  - This fix (PR pending as of this note) needs one more `workflow_dispatch` run to confirm WNS actually populates end-to-end - not yet closed out.
- [x] 3.4 **Done (2026-09-02).** `gh api repos/wortexx/newt/branches/main/protection --jq '.required_status_checks.contexts'` → `["lint","sw"]` — the synth job is not present, confirming it's not a required check.

## 4. Documentation and closeout

- [ ] 4.1 Update `docs/infra-plan.md` Phase 4 (and note in Phase 5's section that its VM was partially pulled forward — cross-reference so Phase 5 isn't planned as if starting from zero): check off the delivered items, record the GitHub-hosted-large-runner dead end and the self-hosted Azure pivot (task 1.1), the VM/subscription details and cost decision (1.1a–1.1c), the fork-PR safety setting (1.1d), the STA invocation from 1.2, and the nightly-cron cost lever. Verify: Phase 4 (and the Phase 5 cross-reference) reflects reality post-merge.
- [ ] 4.2 At sync/archive time, update `openspec/specs/ci-pipeline/spec.md`'s Purpose paragraph to cover the scheduled/on-demand synth lane (deltas don't carry Purpose edits — this is the manual step the spec instructions call out). Verify: purpose sentence mentions the scheduled lane after archive.
