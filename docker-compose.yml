version: '3'

services:
  newt-eda:
    image: ghcr.io/wortexx/newt-eda:dev
    user: "${UID}:${GID}"
    volumes:
      - ./:/app
      - /app/docker # exclude docker folder
    stdin_open: true
    tty: true
    command: /bin/bash
    working_dir: /app
